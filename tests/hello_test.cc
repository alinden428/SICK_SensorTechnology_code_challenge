#include "gtest/gtest.h"
#include "../code_challenge/code_challenge.h"

bool createBot() {
	myRobot newRobot = myRobot();
}

TEST(RobotTests, DemonstrateMacros) {
	EXPECT_EQ(false, false);
	EXPECT_EQ(true, false);
}

TEST(RobotTests, AnotherOne) {
	
	EXPECT_EQ(false, false);
	EXPECT_EQ(true, false);
}

